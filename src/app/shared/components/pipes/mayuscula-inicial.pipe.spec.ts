import { MayusculaInicialPipe } from './mayuscula-inicial.pipe';

describe('MayusculaInicialPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusculaInicialPipe();
    expect(pipe).toBeTruthy();
  });
});
